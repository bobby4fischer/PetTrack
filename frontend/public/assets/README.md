# Public Assets

Place your static assets here so they are served at `/assets/...`.

- Images: put files under `images/` (e.g., `/assets/images/rip.png`).
- Audio: put files under `audio/` (e.g., `/assets/audio/track.mp3`).

These paths match references used in the app (FocusTimer and RIP image).