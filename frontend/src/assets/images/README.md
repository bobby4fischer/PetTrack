Place your images here.

- Expected RIP image path used in code: `src/assets/images/rip.png`
- You can replace it with your own file and name.

Tip: With Vite, static image imports resolve from `src/assets`. The current code references `/assets/images/rip.png` via an `<img>` tag; ensure the file exists to render properly.