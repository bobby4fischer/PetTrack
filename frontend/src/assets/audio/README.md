Place your audio files here.

- Expected music track path referenced: `src/assets/audio/track.mp3`
- Provide any `.mp3` or `.wav` you prefer; the player loops it.

Note: The code uses an `<audio>` element with `src="/assets/audio/track.mp3"`. Ensure the file exists for playback.